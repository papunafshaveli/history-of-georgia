import { Dimensions } from "react-native";

export const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } =
  Dimensions.get("window");

export const DESIGN_SCREEN_WIDTH = 414;
export const DESIGN_SCREEN_HEIGHT = 896;
